export function Post(props) {
    return (
        <div>
            <span>{props.title}</span>
            <span>{props.title}</span>
            <span>{props.created}</span>
            <span>{props.updated}</span>
        </div>
    );
}
